export const CITIZEN_RADIUS = 7;
export const CITIZEN_VISION_RANGE = 30; // visual perception radius in world units
export const MIN_ZOOM = 0.05;
export const MAX_ZOOM = 4.0;
export const WORLD_SCALE = 4; // world units → pixels (1 world unit = 4px)
export const DEFAULT_WALKING_SPEED = 1.4; // visual smoothing fallback if not in API summary
